# Keep instruction and source files byte-identical across Windows and macOS.
# Without this, Windows checkouts can introduce CRLF, causing noisy diffs and
# subtly different files for a mixed-OS team.
*.md  text eol=lf
*.py  text eol=lf
