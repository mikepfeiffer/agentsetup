{
  "name": "agentsetup",
  "version": "0.1.0",
  "private": true,
  "description": "Reference repo for sharing agent instructions and hooks across Claude Code and OpenAI Codex.",
  "type": "module",
  "scripts": {
    "test:payments-app": "npm --prefix examples/payments-app test",
    "test:catalog-api": "dotnet test examples/catalog-api/CatalogApi.sln && npm --prefix examples/catalog-api run test:hooks"
  }
}
