{
  "name": "agentsetup",
  "version": "0.1.0",
  "private": true,
  "description": "Reference repo for sharing agent instructions and hooks across Claude Code and OpenAI Codex.",
  "type": "module",
  "scripts": {
    "build:example": "npm --prefix examples/payments-app run build",
    "test:example": "npm --prefix examples/payments-app test"
  }
}
