# Copy to `.env` and fill in locally. NEVER commit a real .env — it is git-ignored
# by convention AND the shared PreToolUse hook blocks agents from reading/editing
# `.env` files (see ../../.agent-hooks/policy.json).
#
# These are placeholders only — not real secrets.
PAYMENTS_API_KEY=replace-me
DEFAULT_CURRENCY=USD
