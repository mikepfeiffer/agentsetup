{
  "name": "@agentsetup/payments-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Reference app: an idempotent, integer-cents charge ledger that demonstrates the shared agent setup and hooks.",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "npm run test:app && npm run test:hooks",
    "test:app": "tsc && node --test dist/test",
    "test:hooks": "node .agent-hooks/agent-hook.test.mjs",
    "start": "node dist/src/cli.js"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.9.3"
  }
}
